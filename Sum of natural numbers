[]Statement -- Calculate the sum of natural number entered by user

[]CODE:

 #include <stdio.h>
int main() {
    int n, i, sum = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &n);

    for (i = 1; i <= n; ++i) {
        sum=sum+i;
    }

    printf("Sum = %d", sum);//printing final sum value
    return 0;
}

